{
    "name": "sajid-ai-web",
    "version": "1.0.0",
    "description": "Sajid AI web source — also used for Capacitor Android build",
    "private": true,
    "scripts": {
        "android:sync": "npx cap sync android",
        "android:open": "npx cap open android",
        "android:run": "npx cap run android"
    },
    "dependencies": {
        "@capacitor/android": "^6.0.0",
        "@capacitor/core": "^6.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^6.0.0"
    }
}